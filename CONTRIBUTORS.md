# CONTRIBUTORS

## Benchmarking

Use:

```bash
java -jar -Djavawhitelist.d
ebugTiming=true target/javaWhitelist.jar [-allowlist allowlist.txt --ignore .javawhitelistignore] --ignore ../CSE122/.jav
awhitelistignore <paths...> 
```

To get runtime outputs.
